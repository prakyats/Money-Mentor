Money Mentor Backend - Required Production Inputs
=================================================

Please provide these values later (do NOT commit real secrets to git).

1) Database
-----------
DATABASE_URL=
Format example:
postgresql://username:password@host:5432/database_name

Notes:
- Must be production Postgres connection string.
- SSL requirement (yes/no):
- Connection pool requirements:

2) JWT/Auth Secrets
-------------------
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

Notes:
- Use strong random secrets (32+ chars).
- Store only in Render environment variables.

3) CORS Origins
---------------
CORS_ORIGIN=
Expected format (comma-separated):
http://localhost:5173,https://your-app-name.vercel.app,https://*.vercel.app

Your final production values:
- Primary Vercel domain:
- Preview domain policy:

4) Sentry (Optional but recommended)
------------------------------------
SENTRY_DSN=

Notes:
- If not available now, leave empty and add later.

5) xAI / Grok
-------------
AI_PROVIDER=auto
XAI_API_KEY=
XAI_MODEL=grok-4-0709
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

Notes:
- XAI_API_KEY is required for chatbot responses.
- XAI_MODEL can be overridden later if you want a different Grok model.
- AI_PROVIDER controls provider order (`auto` tries xAI first, then Ollama).
- OLLAMA_* values are used when fallback or Ollama-only mode is enabled.

6) Admin Bootstrap (one-time)
-----------------------------
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_FULL_NAME=Platform Admin

Notes:
- ADMIN_PASSWORD must be at least 12 characters.
- After first deploy, run admin seed command once.

7) Render Deployment Inputs
---------------------------
Render service name:
Render region:
Render plan:
Auto-deploy branch:

8) Infrastructure Ownership
---------------------------
Who will manage these in production?
- Render env vars owner:
- DB owner:
- DNS/domain owner:
- Incident alert owner:

9) Compliance / Security Preferences
------------------------------------
- Data retention policy:
- Audit log retention:
- Backup frequency:
- IP allowlist needed (yes/no):
- Any regulatory requirements (GDPR, etc):

10) Optional External Integrations
---------------------------------
If used later, provide keys:
- OpenAI API key (for AI insights):
- Email provider key (SendGrid/Resend/etc):
- SMS provider key (Twilio/etc):

11) Sign-off Checklist
----------------------
[ ] Production DATABASE_URL provided
[ ] JWT secrets provided
[ ] CORS origins finalized
[ ] Admin seed credentials provided
[ ] SENTRY_DSN provided (or explicitly deferred)
[ ] XAI_API_KEY provided
[ ] Render deployment details provided
